language: python
python:
    - "2.7"
install:
    - "pip install coveralls"
    - "./test/bootstrap.sh"
script: nosetests --with-coverage --cover-package=preferences,rbhandle,serve,web,rhythmweb 
after_success:
    - "coveralls"
